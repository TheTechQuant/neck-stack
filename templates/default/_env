# Operator-editable settings.
DOMAIN=__DOMAIN__
PROD_PLATFORM=__PROD_PLATFORM__
KOMODO_URL=__KOMODO_URL__
KOMODO_SERVER=__KOMODO_SERVER__

# Local secrets.
SIGNOZ_USER_ROOT_EMAIL=__SIGNOZ_ROOT_EMAIL_DEFAULT__
SIGNOZ_USER_ROOT_PASSWORD=__SIGNOZ_ROOT_PASSWORD_DEFAULT__
SIGNOZ_USER_ROOT_ORG_NAME=NECK

# pnpm komodo:setup asks for KOMODO_API_KEY / KOMODO_API_SECRET once and saves them here.

# Optional runtime overrides. Only add these after introducing matching Encore resources.
# POSTGRES_PASSWORD=replace-generated-default
# REDIS_PASSWORD=replace-generated-default
# Add Encore secret env vars here when `pnpm neck infra` reports them.
